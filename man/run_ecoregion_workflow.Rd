% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_main_workflow.R
\name{run_ecoregion_workflow}
\alias{run_ecoregion_workflow}
\title{Main workflow function — one call does everything}
\usage{
run_ecoregion_workflow(
  shapefile,
  continuous_rasters,
  categorical_rasters,
  id_col = "ID",
  unit_col = "ADM2_NAME",
  group_col = "ADM0_NAME",
  country_name = NULL,
  study_groups = NULL,
  continuous_funs = NULL,
  agg_facts = 1,
  lc_exclude_vals = c(10, 4294967295, 4294967296),
  numeric_vars = NULL,
  min_k = 6,
  max_k = 15,
  pca_var = 0.9,
  nstart = 50,
  seed = 1,
  visualize = TRUE,
  output_dir = "output",
  projection = "+proj=moll",
  viridis_option = "turbo",
  cat_threshold = 0.01,
  altitude_breaks = c(500, 1500)
)
}
\arguments{
\item{shapefile}{Path to the admin polygon shapefile.}

\item{continuous_rasters}{Named list of continuous raster file paths.
Names become column prefixes (e.g. list(elev = "elev.tif", pop = "pop.tif")).}

\item{categorical_rasters}{Named list of categorical raster file paths.
Must include elements named \code{kg} and \code{landcover}.}

\item{id_col}{Name of the unique polygon ID column.
Created automatically as a sequential integer if absent. Default \code{"ID"}.}

\item{unit_col}{Name of the sub-unit label column (e.g. district
or county name). Used in summary tables and popup labels.}

\item{group_col}{Column name for grouping (e.g. \code{"ADM0_NAME"}).
Set \code{NULL} for single-country mode (requires \code{country_name}).}

\item{country_name}{Country label used when \code{group_col = NULL}.}

\item{study_groups}{Character vector of group values to keep.
\code{NULL} keeps all rows (required when \code{group_col = NULL}).}

\item{continuous_funs}{Named list of summary functions per raster.
Each element is itself a named list, e.g.
\code{list(elev = list(median = median), pop = list(mean = mean))}.
If \code{NULL} (default), \code{mean} is applied to every raster.}

\item{agg_facts}{Named list of aggregation factors (integer) per
raster. Use values > 1 to speed up extraction of high-resolution rasters.
Default \code{1} for all rasters (no aggregation).}

\item{lc_exclude_vals}{Integer vector of land-cover raster values to mask
before extraction (e.g. clouds, nodata). Default removes clouds (10) and
common 32-bit sentinel values.}

\item{numeric_vars}{Character vector of numeric covariate column names
as they appear after extraction (e.g. \code{c("elev_median","pop_mean")}).
If \code{NULL} (default), auto-detected via \code{_mean|_median|_max}.}

\item{min_k}{Minimum number of clusters. Default \code{6}.}

\item{max_k}{Maximum number of clusters to test. Default \code{15}.}

\item{pca_var}{Cumulative variance threshold for PC retention.
Default \code{0.90}.}

\item{nstart}{k-means nstart for the final run. Default \code{50}.}

\item{seed}{Random seed. Default \code{1}.}

\item{visualize}{Logical. Print PCA/silhouette/consensus plots during
clustering. Default \code{TRUE}.}

\item{output_dir}{Path to the output directory. Created if absent.
Default \code{"output"}.}

\item{projection}{PROJ string for the static map.
Default Mollweide (\code{"+proj=moll"}).}

\item{viridis_option}{Viridis palette name for maps. Default \code{"turbo"}.}

\item{cat_threshold}{Minimum mean proportion for a category to appear in
cluster descriptions. Default \code{0.01}.}

\item{altitude_breaks}{Numeric vector of two elevation breakpoints (m) used
to assign lowland / mid-altitude / highland labels. Default \code{c(500, 1500)}.}
}
\value{
A named list with elements:
\describe{
  \item{clusters}{Data frame with cluster assignments and PC scores.}
  \item{adm2_clusters}{sf object — polygons joined with cluster and popup fields.}
  \item{env_df}{Wide covariate table used for clustering.}
  \item{cluster_desc}{Compact cluster summary data frame.}
  \item{bioregion_text}{Data frame with a \code{paragraph} column per cluster.}
  \item{leaflet_map}{Leaflet map object.}
  \item{static_map}{ggplot map object.}
  \item{heatmap_plots}{Named list of heatmap ggplot objects (one per group).}
}
}
\description{
Runs the full ecoregion pipeline: load polygons → extract raster stats →
cluster → visualise → export maps, tables, and text descriptions.
}
