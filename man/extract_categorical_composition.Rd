% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_extract_categorical.R
\name{extract_categorical_composition}
\alias{extract_categorical_composition}
\title{Compute the proportion of each category within each polygon for a
categorical SpatRaster.  Returns a wide tibble (one row per polygon).}
\usage{
extract_categorical_composition(
  polygons,
  cat_raster,
  lookup = NULL,
  exclude_vals = NULL,
  id_col = "ID",
  prefix = ""
)
}
\arguments{
\item{polygons}{sf object.}

\item{cat_raster}{SpatRaster with integer / factor values.}

\item{lookup}{Named character vector mapping raster values to labels.
Set to NULL to keep numeric codes.}

\item{exclude_vals}{Integer vector of raster values to exclude (e.g. clouds).}

\item{id_col}{Name of the row ID column in \code{polygons}.}

\item{prefix}{Character prefix added to every output column name.}
}
\value{
A tibble: [id_col] + one column per category (proportions 0-1).
}
\description{
Compute the proportion of each category within each polygon for a
categorical SpatRaster.  Returns a wide tibble (one row per polygon).
}
