% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_skater_clustering.R
\name{plot_skater_elbow}
\alias{plot_skater_elbow}
\title{Elbow plot to guide SKATER cluster count selection}
\usage{
plot_skater_elbow(
  polygons_sf,
  vars = NULL,
  max_k = 20,
  pca_var = 0.95,
  elev_col = NULL,
  elev_weight = 7,
  dist_threshold = 45000,
  k_fallback = 1,
  seed = 42
)
}
\arguments{
\item{polygons_sf}{An \code{sf} object previously processed by
\code{\link{run_skater_clustering}}, **or** any \code{sf} object if
\code{vars} and \code{pca_var} are supplied directly.}

\item{vars}{Character vector of covariate column names.  If
\code{polygons_sf} has a \code{"vars"} attribute (set automatically by
\code{run_skater_clustering}), this argument can be omitted.}

\item{max_k}{Maximum number of clusters to evaluate.  Default \code{20}.}

\item{pca_var}{Cumulative variance retained by PCA.  Default \code{0.95}.}

\item{elev_col}{Optional elevation column name (must match what was used
in \code{run_skater_clustering}).  Default \code{NULL}.}

\item{elev_weight}{Elevation boost multiplier.  Default \code{7}.}

\item{dist_threshold}{Distance threshold for neighbour graph.  Default \code{45000}.}

\item{k_fallback}{Forced NN count for isolated polygons.  Default \code{1}.}

\item{seed}{Random seed.  Default \code{42}.}
}
\value{
A \code{ggplot} object (the elbow curve).  Also prints the plot.
}
\description{
Computes the within-cluster sum of squared errors (SSE) in PCA-score space
for \code{k = 1} to \code{max_k} and plots the result.  The optimal number
of clusters is typically at the "elbow" — the point where adding another
cluster yields diminishing SSE reductions.
}
\examples{
\dontrun{
plot_skater_elbow(adm2_sf, vars = my_vars, max_k = 20)
}

}
\seealso{
\code{\link{run_skater_clustering}}
}
