% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_paragraphs.R
\name{generate_cluster_paragraphs}
\alias{generate_cluster_paragraphs}
\title{Generate human-readable paragraphs describing each cluster.}
\usage{
generate_cluster_paragraphs(
  summary_df,
  numeric_vars,
  lc_classes = c("Trees", "Crops", "Built Area", "Rangeland", "Water", "Bare ground",
    "Flooded vegetation", "Snow/Ice"),
  altitude_col = NULL,
  altitude_breaks = c(500, 1500),
  altitude_labels = c("Lowland (< 500 m)", "Mid-altitude (500–1500 m)",
    "Highland (≥ 1500 m)"),
  group_label = "Group"
)
}
\arguments{
\item{summary_df}{Output of summarize_clusters_compact().}

\item{numeric_vars}{Character vector of numeric covariate names
(must match column prefixes in summary_df).}

\item{lc_classes}{Character vector of land-cover class names
(used to separate climate from LC in categories).}

\item{altitude_col}{The numeric_var whose range is used to assign
altitude labels.  Set NULL to skip.}

\item{altitude_breaks}{Named numeric vector of breaks:
c(lowland_max = 500, midalt_max = 1500).}

\item{altitude_labels}{Named character vector of labels for each band.}

\item{group_label}{Column name for group (default "Group").}
}
\value{
The summary_df with an extra `paragraph` column.
}
\description{
Generate human-readable paragraphs describing each cluster.
}
