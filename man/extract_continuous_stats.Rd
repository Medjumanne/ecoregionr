% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_extract_continuous.R
\name{extract_continuous_stats}
\alias{extract_continuous_stats}
\title{Extract summary statistics from one or more continuous rasters per polygon.}
\usage{
extract_continuous_stats(
  polygons,
  rasters,
  funs = list(mean = mean),
  id_col = "ID",
  aggregate_fact = 1
)

extract_continuous_stats(
  polygons,
  rasters,
  funs = list(mean = mean),
  id_col = "ID",
  aggregate_fact = 1
)
}
\arguments{
\item{polygons}{sf object – the administrative / study polygons.}

\item{rasters}{Named list of SpatRaster objects.
Names become column prefixes in the output.}

\item{funs}{Named list of functions to apply (one per raster).
Can be a single named list recycled to all rasters,
or a list-of-lists with one entry per raster.
Default: list(median = median) applied to every raster.}

\item{id_col}{Name of the unique row ID column in `polygons`.}

\item{aggregate_fact}{Integer. If > 1, aggregate rasters by this factor
before extraction (speeds up large rasters).
Recycled: supply a named list for per-raster control.}
}
\value{
The input `polygons` sf object with extra columns appended.
}
\description{
Extract summary statistics from one or more continuous rasters per polygon.
}
\examples{
\dontrun{
  polys <- extract_continuous_stats(
    polygons = adm2,
    rasters  = list(elev = dem, pop = pop, urban = urban),
    funs     = list(elev  = list(median = median),
                    pop   = list(mean   = mean),
                    urban = list(mean   = mean)),
    aggregate_fact = list(elev = 1, pop = 1, urban = 10),
    id_col = "ID"
  )
}
}
